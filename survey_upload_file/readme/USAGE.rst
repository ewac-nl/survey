The employee configuring a survey can now set the survey question type to
'File Upload'. This shows a file upload widget when a user is answering the
survey. The file is stored as such in the survey user input.
